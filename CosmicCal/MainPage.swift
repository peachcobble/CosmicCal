import SwiftUI

struct MainPage: View {
    var body: some View {
        VStack {
            Text("Welcome to CosmicCal")
                .font(.title)
                .padding(.top, 50)
            Spacer()
        }
    }
}
